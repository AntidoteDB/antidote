language: erlang
otp_release:
  - 18.3
  - 19.1
install:
  - make
  - ./rebar3 update
script:
  - make test
  - make dialyzer
  - make lint
  - echo "{plugins, [rebar3_proper]}." > ~/.config/rebar3/rebar.config
  - ./rebar3 proper -n 1000
sudo: required
dist: trusty
