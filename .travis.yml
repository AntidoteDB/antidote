language: erlang
otp_release:
  - 18.3
  - 19.1
install:
  - make
  - ./rebar3 update
script:
  - make systests
notifications:
  email: christopher.meiklejohn@gmail.com
sudo: required

env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8