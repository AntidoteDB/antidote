datasources:
- name: 'prometheus'
  type: 'prometheus'
  access: 'proxy'
  org_id: 1
  # locally reachable prometheus endpoint if on host network
  # docker container name if in docker network
  url: 'prometheus:9090'
  is_default: true
  version: 1
  editable: false
