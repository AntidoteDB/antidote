The all.sh script will:
1. create N releases of antidote
2. start N instances of antidote in a local machine
3. join those instances into a cluster.

To launch a cluster, just run, from the antidote's directory:

./bin/all.sh <N>

NOTE: current maximum of N is 5.